{
  "name": "git-map",
  "version": "1.3.0",
  "private": true,
  "description": "Dependency-free visual Git guide for beginners.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node scripts/validate.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
