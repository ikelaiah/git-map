{
  "name": "git-map",
  "version": "1.4.0",
  "private": true,
  "description": "Visual Git sandbox for mapping status, experimenting with branches, and recovering from common mistakes.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node scripts/validate.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
